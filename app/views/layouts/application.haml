!!!
%html
  %head
    %title Picasso
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body

    %header.bob

      .row
        .cell.well

          .g
            .g-b.g-b--l--2of12

              %h3.dib
                = link_to 'Picasso', root_path

            .g-b.g-b--l--10of12.tar

              %nav
                - if user_signed_in?
                  = link_to 'Sign Out', destroy_user_session_path, :method => :delete, class: 'dib'
                  = link_to 'Account', edit_user_registration_path, class: 'dib mlm'
                  = link_to 'Submit Question', new_question_path, class: 'dib mlm'
                - else
                  = link_to 'Sign Up', new_user_registration_path, class: 'dib'
                  = link_to 'Sign In', new_user_session_path, class: 'dib mlm'

    / TODO: Structure thse
    %p.notice= notice
    %p.alert= alert

    %main= yield
